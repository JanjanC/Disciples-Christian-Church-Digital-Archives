{{#each this}}
<div class='col-4' style='margin-bottom: 1em;' value="{{member_id}}, {{person_id}}, {{first_name}}, {{middle_name}}, {{last_name}}">
    <div class='card witness' data-member-info=>
        <div class='card-body'>
            <p class='card-text'> {{first_name}} {{middle_name}}. {{last_name}}</p>
            <button type='button' class='fas fa-trash delGMotherWitnessBtn'>
            </button> 
        </div>
     </div>
 </div>
 {{/each}}